# Cursor Build Prompt (copy/paste)

(Include the build prompt for Next.js v0.1: routes /, /diagram, /show, /saved; voice input; rule-based PlayPlan; SVG render; PNG export; share sheet; print CSS; local save last 20 plays.)
