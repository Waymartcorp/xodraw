# PlayPlan Schema (v0.1)

XODraw converts voice/text into a structured PlayPlan JSON, then renders SVG.

Include: sport, canvas/view, title, notes, actors (id,label,x,y), puckOrBall position, actions (MOVE/PASS/SHOT), style.

Add one example JSON for hockey breakout (use the same sample we discussed earlier).
